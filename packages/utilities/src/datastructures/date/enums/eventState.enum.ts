export enum NGSEventState {
  Requested = "requested",
  // Pending = 'pending',
  Confimred = "confirmed",
  CancelRequest = "cancelRequest",
  // Declined = 'declined',
  Canceled = "canceled",
  Planing = "planing"
}
