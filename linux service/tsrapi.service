[Unit]
Description=TSRadio NodeJS API
After=network.target

[Service]
Type=simple

User=user
Group=group

ExecStart=/usr/bin/npm --prefix /path/to/api run dev

Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target